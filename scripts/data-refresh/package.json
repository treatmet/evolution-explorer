{
  "name": "@evo-tree/data-refresh",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "refresh": "tsx src/index.ts",
    "promote": "tsx src/index.ts --promote-approved",
    "refresh:media": "tsx src/index.ts --media-online",
    "promote:media": "tsx src/index.ts --promote-approved --media-online",
    "build": "tsc -b",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@evo-tree/scientific-data": "0.1.0"
  },
  "devDependencies": {
    "tsx": "^4.20.5"
  }
}
