import type { NodeNameForm } from '@evo-tree/domain';

/** Display form for node labels. Precomputed on every node, so this can move to the UI later. */
export const DEFAULT_NAME_FORM: NodeNameForm = 'singular';
