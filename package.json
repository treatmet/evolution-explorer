{
  "name": "evo-tree",
  "version": "0.1.0",
  "private": true,
  "description": "EVO-TREE monorepo",
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*",
    "scripts/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test",
    "lint": "npm run lint --workspaces --if-present",
    "start": "npm --workspace @evo-tree/web run dev",
    "data": "npm --workspace @evo-tree/data-refresh run refresh -- --progress --progress-interval-percent=5 --description-max-chars=500"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.1",
    "@types/node": "^24.13.3",
    "typescript": "~6.0.2",
    "vitest": "^3.2.4"
  }
}
